/// App-wide constants (e.g. OSM tile User-Agent for flutter_map).
class AppConstants {
  AppConstants._();
  static const String osmUserAgent = 'KigaliCityServices/1.0';
  /// Package name for OSM TileLayer userAgentPackageName (identifies the app to tile servers).
  static const String osmPackageName = 'com.example.kigali_city_services';
}
